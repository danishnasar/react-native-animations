import { StyleSheet } from "react-native";
export const SQUARE_SIZE = 100;
export const CIRCLE_SIZE = 350;
export const CIRCLE_RADIUS = 350/2;

export const styles = StyleSheet.create({
    container: {
      flex: 1,
    }
  });